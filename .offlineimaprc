[general]
accounts = Gmail
maxsyncaccounts = 1
pythonfile = ~/.offlineimap.py

[Account Gmail]
localrepository = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/Maildir

[Repository Remote]
type               = Gmail
remoteuser         = stites.sr@gmail.com
remotepasseval     = keyring.get_password("gmail", "stites.sr@gmail.com")
realdelete         = no
remoteport         = 993
ssl                = yes
maxconnections     = 3
keepalive          = 60
holdconnectionopen = yes
#holdconnectionopen = true
sslcacertfile      =/usr/share/curl/ca-bundle.crt
folderfilter       = lambda foldername: foldername not in ['[Gmail]/Spam', '[Gmail]/All Mail', '[Gmail]/Starred', '[Gmail]/Important']

