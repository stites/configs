#!/bin/bash
# ============================================= #
# DEPRECIATED                                   #
# ============================================= #
# 
# 
# 
# if [ ! -d "$HOME/.jenv" ]; then
#   git clone https://github.com/gcuisinier/jenv.git ~/.jenv
# fi
# 
# safe_path_add $HOME/.jenv/bin
# 
# if which jenv > /dev/null; then eval "$(jenv init -)"; fi
# 
# #MAC_PATH=/Library/Java/JavaVirtualMachines
# #for jdk in `ls $MAC_PATH`; do
# #  JDK_PATH=$MAC_PATH/$jdk/Contents/Home
# #  jenv add $JDK_PATH
# #doneless
# # finally, add ja | help
# maybe include thijava`
# export JAVA_HOME="/Library/Java/JavaVirtualMachines/jdk1.8.0_25.jdk/Contents/Home"
MAVEN_OPTS="-XX:+TieredCompilation -XX:TieredStopAtLevel=1 -Xms1024m -Xmx4096m -XX:PermSize=1024m"
function mvn_install {
  mvn install -pl $1 -am —offline
}
export -f mvn_install


